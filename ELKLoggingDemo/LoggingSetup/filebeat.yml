filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - "/var/log/dotnet/*.log"  # Adjust to your actual log file location
    json.keys_under_root: true
    json.add_error_key: true

output.logstash:
  hosts: ["logstash:5044"]
